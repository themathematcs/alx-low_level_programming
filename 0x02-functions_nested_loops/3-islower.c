#include "main.h"
/**
 * _islower -> checks if the lower case character
 * _islower - Returns 1 if character is l;owercase and 0 if not
 * @c: the character to check
 *
 * return: 0
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
