#include <stdio.h>
/**
 * main -check the code
 * 
 * print_alphabet -prints char a to z in lower case
 *
 * Return: always 0.
 */
void print_alphabet(void);
{
int main(void)
print_alphabet();
putchar('\n');
return (0);
}
void print_alphabet(void)
{
char ch;
for (ch == 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
return (ch);
}
