#include "main.h"
/**
 * add -> adds two numbers returning a value
 * @a: added int to b
 * @b: int added to a
 * Return: the sum of a and b
 */
int add(int a, int b)
{
int n;
n = a + b;
return (c);
}
