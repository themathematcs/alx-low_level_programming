#include "main.h"
/**
 * _strcat -> concatinates two strings
 * @dest: character argument
 * @src: string add
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
int x, m;
x = 0;
m = 0;
while (dest[i] != '\0')
i++;
while (src[j] != '\0')
{
j++;
i++;
}
dest[i] = '\0';
return (dest);
}
